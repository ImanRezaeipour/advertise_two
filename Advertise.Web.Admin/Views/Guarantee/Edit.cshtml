@* Generator: MvcView GeneratePrettyNames: true *@
@model Advertise.Core.Models.Guarantee.GuaranteeEditViewModel
@{
    Layout = MVC.Shared.Views.SiteLayout.Panel._PanelLayout;
    ViewBag.Title = "ایجاد کاتالوگ";
}
<div class="profile-content">
    <div class="headline buttons primary">
        <h4>ایجاد برند</h4>
    </div>

    <div class="form-box-item profile-form">
        <form id="newGuarantee" action="@Url.Action(MVC.Guarantee.Edit())" method="post" data-on-load="validateGuarantee">
            @Html.AntiForgeryToken()
            @Html.ValidationSummary()

            <div class="input-form">
                <div class="vertical-dashed"></div>
                
                <div class="msg-container">
                    <p class="msg-box regular round-corners-five"></p>
                </div>
                <div class="input-container fixed-height">
                    <label for="Title" class="rl-label">
                        <span>@Admin.Title:</span>
                    </label>
                    <input type="text" id="Title" value="@Model.Title" name="Title"/>
                </div>
                
                <div class="msg-container">
                    <p class="msg-box regular round-corners-five"></p>
                </div>
                <div class="input-container fixed-height">
                    <label for="Description" class="rl-label">
                        <span>@Admin.Description:</span>
                    </label>
                    <input type="text" id="Description" value="@Model.Description" name="Description"/>
                </div>
                
                <div class="msg-container">
                    <p class="msg-box regular round-corners-five"></p>
                </div>
                <div class="input-container fixed-height">
                    <label for="Email" class="rl-label">
                        <span>@Admin.Email:</span>
                    </label>
                    <input type="text" id="Email" value="@Model.Email" name="Email"/>
                </div>
                
                <div class="msg-container">
                    <p class="msg-box regular round-corners-five"></p>
                </div>
                <div class="input-container fixed-height">
                    <label for="MobileNumber" class="rl-label">
                        <span>@Admin.Mobile:</span>
                    </label>
                    <input type="text" id="MobileNumber" value="@Model.MobileNumber" name="MobileNumber"/>
                </div>
                
                <div class="msg-container">
                    <p class="msg-box regular round-corners-five"></p>
                </div>
                <div class="input-container fixed-height">
                    <label for="PhoneNumber" class="rl-label">
                        <span>@Admin.Phone:</span>
                    </label>
                    <input type="text" id="PhoneNumber" value="@Model.PhoneNumber" name="PhoneNumber"/>
                </div>

                <div class="input-container"></div>
            </div>
        </form>
        
        <div class="form-button-container">
            <button type="submit" form="newGuarantee" class="form-button right-form-btn ok-button round-corners-five disabled-btn-link" disabled="disabled">
                <i class="fa fa-save"></i>
                <span>@Admin.Save</span>
            </button>
        </div>
    </div>
</div>
