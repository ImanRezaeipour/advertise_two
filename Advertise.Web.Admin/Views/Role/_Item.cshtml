@* Generator: MvcView GeneratePrettyNames: true *@
@using Advertise.Core.Extensions
@using Advertise.Service.Managers.File
@using Advertise.Service.Services.Permissions
@model Advertise.Core.Models.Role.RoleViewModel
<div class="landing-items nov-item-height-020">
    <div class="product-item column landing-column">
        <div class="nov-item-header">
            <div class="action-section without-share">
                @if (User.IsInRole(PermissionConst.CanRoleEdit) || User.IsInRole(PermissionConst.CanRoleDeleteAjax))
                {
                    <div class="item-edit-wrapper">
                        <i class="fa fa-pencil-square"></i>
                        <ul class="item-edit-list">
                            @if (User.IsInRole(PermissionConst.CanRoleEdit))
                            {
                                <li>
                                    <a href="@Url.Action(MVC.Role.Edit(Model.Id))">
                                        <span>@Admin.Edit</span>
                                        <i class="fa fa-pencil fa-flip-horizontal"></i>
                                    </a>
                                </li>
                            }
                            @if (User.IsInRole(PermissionConst.CanRoleDeleteAjax))
                            {
                                <li>
                                    <a href="javascript:void(0)" data-on-click="removeConfirm" data-param="@Url.Action(MVC.Role.DeleteAjax(Model.Id))">
                                        <span>@Admin.Delete</span>
                                        <i class="fa fa-trash"></i>
                                    </a>
                                </li>
                            }
                        </ul>
                    </div>
                }
            </div>
        </div>
        <div class="nov-item-body">
            <div class="image-section">
                <div class="product-preview-actions">
                    <figure class="product-preview-image">
                        <div class="item-preview-image">
                            <img src="@FileConst.Role" alt="@($"{Admin.UserRole} {Model.Name}")">
                        </div>
                    </figure>
                </div>
            </div>
            <div class="caption-section">
                <a class="novinak-title-link">
                    <p class="novinak-title text-header">@Model.Name</p>
                </a>
            </div>
            <div class="in info-section">
                <div class="middle-part">
                    <p>
                        <i class="fa fa-calendar-check-o"></i>
                        <span>@Admin.InsertDate.PlusWord(":")</span>
                    </p>
                </div>
                <div class="bottom-part">
                    <p>@Model.CreatedOn.CastToRegularDate()</p>
                </div>
            </div>
        </div>
        <div class="clearboth"></div>
    </div>
</div>